pre-commit:
  parallel: true
  commands:
    rust-fmt:
      glob: "*.rs"
      run: cargo fmt -- --check
    ts-lint:
      glob: "dashboard/src/**/*.{ts,tsx}"
      root: "dashboard/"
      run: npx biome check {staged_files}
